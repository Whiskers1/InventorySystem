<UserControl x:Class="InventorySystem.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InventorySystem.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="0.5*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="1" BorderThickness="1" BorderBrush="Gray" Background="#eee" CornerRadius="8,8,8,8" VerticalAlignment="Center">
            <StackPanel  VerticalAlignment="Center" Margin="10">
                <TextBlock Text="User Name"/>
                <TextBox x:Name="uid"/>
                <TextBlock Text="Password" Margin="0 5 0 0"/>
                <PasswordBox x:Name="pass"/>
                <Button Content="Login" x:Name="LoginButton" Margin="0 10 0 0" Click="Button_Click_Login"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
